/** WebView Android : le focus trap MUI avale les taps après clavier / caméra. */
export const webviewOverlayDefaultProps = {
  disableEnforceFocus: true,
  disableAutoFocus: true,
  disableRestoreFocus: true,
} as const;
